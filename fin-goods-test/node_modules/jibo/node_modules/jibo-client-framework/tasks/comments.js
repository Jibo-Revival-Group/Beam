module.exports = function(gulp, options, plugins) {
    gulp.task('comments', function() {
        return gulp.src(options.src)
            .pipe(plugins.comments(options.argv.exclude))
            .pipe(gulp.dest(options.docsDest));
    });
};