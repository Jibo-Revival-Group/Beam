'use strict';

Object.defineProperty(exports, "__esModule", {
    value: true
});

var _jiboClientFramework = require('jibo-client-framework');

var _url = require('url');

var _url2 = _interopRequireDefault(_url);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function getRegistry(cb) {
    let registryHost;
    let ipc = null;
    try {
        ipc = require('ipc');
    } catch (e) {
        cb(e);
        return;
    }

    ipc.on('set-context', function (initData) {
        if (typeof initData === 'string' || initData instanceof String) {
            registryHost = initData;
        } else {
            registryHost = initData.registryHost;

            // The session token isn't used right now, so I'm just commenting this out
            /*if(initData.token) {
                this.session.token = initData.token;
                console.warn('Session Token = ' + this.session.token);
            }*/
        }

        const registryInfo = _url2.default.parse(registryHost);
        console.log('registryInfo');
        console.log(registryInfo);

        _jiboClientFramework.RegistryClient.createInstance(registryInfo.hostname, registryInfo.port);

        _jiboClientFramework.RegistryClient.instance.getRecords(cb);
    });

    ipc.send('get-context');
}

exports.default = getRegistry;
//# sourceMappingURL=../map/services/registry.js.map
