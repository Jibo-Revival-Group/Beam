var Jibo = require('../core');

['putEventsAnon'].forEach(function(method) {
    Jibo.Log.prototype[method] = function(params, callback) {
        return this.makeUnauthenticatedRequest(method, params, callback);
    };
});
