const TestUtils = require('./TestUtils');

describe('packages', function() {

    before(function (done) {
        this.timeout(20000);
        TestUtils.beforeTests(done);
    });

    after(function () {
        TestUtils.afterTests();
    });

    describe('jibo-emotion-system', function() {
        require('./unit');
        require('./integration');
    });
});
