import { TimeOfDay, SleepState } from './Types';
export {  };
/**
 * @typedef jibo.action#ExternalState
 * @description The externally visible action system state.
 * @prop time {Date} Current time.
 * @prop timeOfDay {jibo.action.types#TimeOfDay} Current time of day.
 * @prop sleep {jibo.action.types#SleepState} Jibo's current sleep/wake state.
 */
export interface ExternalState {
    time: Date;
    timeOfDay: TimeOfDay;
    sleep: SleepState;
}
