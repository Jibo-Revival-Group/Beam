/// <reference types="node" />
import { EventEmitter } from 'events';
export declare type ErrCallback = (err?: Error) => void;
declare class NotificationsDispatcher extends EventEmitter {
    private notificationsSocket;
    private statusSocket;
    private lastStatus;
    constructor(host?: string, port?: number);
    init(callback: ErrCallback): void;
    private _processNotification(message);
    private _processStatus(message);
    private _setupSockets(url);
}
export default NotificationsDispatcher;
