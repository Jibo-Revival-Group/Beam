describe('packages', function() {
    describe('jibo-keyframes', function() {
        require('./keyframes/keyframe-search');
        describe('runtime', function() {
            require('./keyframes/runtime-basics');
            require('./keyframes/runtime-evaluate-layer');
            require('./keyframes/runtime-evaluate-all');
        });
        require('./keyframes/tweening');
        require('./keyframes/keyframe-blink-mode');
    });
});
