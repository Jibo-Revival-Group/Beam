/**
 * A class that couples a FlowExecutor with a FlowHarness.
 * It is responsible for determining:
 * 1) which harness to attach,
 * 2) when and where in a flow the harness steps are to be executed, and
 * 3) executing the harness steps.
 */

    /**
     * Execute all the harness steps that are ready, given the current executor's state.
     * @param {State} state the flow executor's current state
     * @returns {State}
     */

    /**
     * Watch the FlowExecutor as it runs, seeing if there's a harness
     * that matches the current state, and attaching it if it does.
     */

    /**
     * Take the indexed harness off the list and set it up.
     * @param index
     * @private
     */

    /**
     * Keep track of all the activities encountered by the executor, for later use by the harness.
     * @private
     */

    /**
     * Do any setup required to run the harness.
     */

    /**
     * Check to see if the flow executor (and environs) is in a state that matches the next step in the harness.
     * @param state
     * @param stepNumber
     * @returns {boolean}
     * @private
     */

    /**
     * Return true if a Listener has subscribed to get speech input.
     * @param step
     * @returns {boolean}
     * @private
     */

    /**
     * Has the flow executor executed the activity described by the step the number of times specified
     * by the step?
     * @param step
     * @private
     */

    /**
     * Execute the next step in the harness.
     * @param state
     * @param stepNumber
     * @returns {State}
     * @private
     */