import { EventContainer, Event } from 'jibo-typed-events';
import { Goal } from '../goal/Goal';
/**
 * Defines a set of events that the action system might emit. These will be of a general nature
 * as opposed to the more specific events defined on goal objects for example
 * @class ActionEvents
 * @memberof jibo.action
 * @property {Event} oriented - Event emitted when Jibo is done orienting towards a lookat target
 * @property {Event} goalAdded - Goal was added to system
 * @property {Event} goalCulled - Goal that had been added is being culled
 */
export declare class ActionEvents extends EventContainer {
    oriented: Event<{}>;
    goalAdded: Event<Goal>;
    goalCulled: Event<Goal>;
    constructor();
}
