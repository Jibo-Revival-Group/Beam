// Permissive ambient typings shim for the jibo v14 runtime package.
// The published package does not include generated .d.ts files; this shim lets
// TypeScript skills (e.g. @be/recipe) compile against the runtime, treating the
// jibo API surface as `any`. It has no effect at runtime.
declare const jibo: any;
export = jibo;
